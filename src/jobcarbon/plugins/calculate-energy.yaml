path: builtin
method: Multiply
config:
  input-parameters:
    - duration_hours
    - node_power_kw
  output-parameter: energy
parameter-metadata:
  outputs:
    energy:
      unit: kWh
      description: Node energy consumed during the observation interval. Component sums may not be accurate across nodes
      aggregation-method:
        time: sum
        component: sum
